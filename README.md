# Dog-Cat-Classifier-CNN
A Convolutional Neural Network (CNN) built with TensorFlow/Keras to classify images of dogs and cats. Includes data augmentation, model training, and prediction on new images.
